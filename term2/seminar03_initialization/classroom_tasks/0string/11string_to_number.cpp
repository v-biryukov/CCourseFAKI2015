/*
    Конвертация строки в число.

    Для конвертации из строки в число можно использовать функции 
    stoi (для int), stoll (для long long), stof(для float), stod(для double) и т. д.
*/

#include <iostream>
#include <string>
using std::cout, std::cin, std::endl;


int main()
{
    int c = std::stoi(sa);
    cout << c << endl;

    float d = std::stof(sb);
    cout << d << endl;
}