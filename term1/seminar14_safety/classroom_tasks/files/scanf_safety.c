#include <stdio.h>


int main()
{
	char a[5] = "Lion";
	char b[5];

	scanf("%s", b);

	printf("a = %s\n", a);
	printf("b = %s\n", b);

	// Что напечатает эта программа, если подать на вход следующие строки:
	// Cat   Zebra   Elephant
	// Почему так происходит?
	// Как это исправить?
}